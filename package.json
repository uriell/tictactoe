{
  "name": "tictactoe",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "firebase-tools": "6.10.0",
    "lerna": "3.14.1"
  },
  "scripts": {
    "bootstrap": "lerna bootstrap",
    "build": "lerna run build",
    "deploy": "firebase deploy"
  }
}
